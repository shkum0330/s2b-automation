jwt:
  token:
    secretKey: ${JWT_TOKEN_SECRETKEY}

logging:
  level:
    org:
      springframework:
        jdbc: debug
        security: info
      com.zaxxer.hikari: debug

kakao:
  client-id: ${KAKA0_CLIENT_ID}
  redirect-uri: ${KAKAO_REDIRECT_URI}
  client-secret: ${KAKAO_CLIENT_SECRET}